import "children"

func decorationHeight() int64 {
	margin := deref_i64("_margin_")
	maxSize := deref_i64("_maxSize_")
	childURI := hasOneChild("_myName_")
	size := int64(30)
	if childURI != "" {
		seg := uri_segment(childURI, 3)
		size = childHeight(seg)
	}
	result := size + margin + margin
	if result > maxSize {
		return maxSize
	}
	return result
}
