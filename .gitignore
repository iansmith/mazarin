# Build output
build/

# Personal scratch space (managed outside git; formerly docs/)
scratch/

# slopstop project-local ticket tracking (transient working state, not source)
.slopstop/

# Binary artifacts
*.o
*.elf
*.test
coverage.out

# Tool environments
tools/fontrender/myenv/

# Cross-compilation toolchain
aarch64-none-elf/

# Local binaries
bin/

# Generated constraint bytecode
*.vbc.go

# IDE/editor
.idea/
.vscode/
*.swp
*~

# Claude Code

# Built .maz binaries
/fat32

# Task checksum cache
.task/

# Generated embedded data (regenerated by task)
cardinal/asm/dev/kmazarin_data_arm64.s
cardinal/asm/dev/boot_mazarin_data_arm64.s

# Test binaries in stack tests
cardinal/stack/*/main
cardinal/stack/*/test
cardinal/stack/test02_asm_bridge/test02_asm_bridge

# Compiled binaries that might appear in source dirs
kmazarin/kmazarin/kmazarin
# Staged embedded files (copied by stage tasks for go:embed; per-arch — MAZ-153)
kmazarin/kmazarin/fs_arm64.elf
kmazarin/kmazarin/fs_amd64.elf
kmazarin/kmazarin/kernel_arm64.toml
kmazarin/kmazarin/kernel_amd64.toml
# Legacy shared-path staged names (pre-MAZ-153); may linger on dev machines
kmazarin/kmazarin/fs.elf
kmazarin/kmazarin/kernel.toml
kmazarin/ds/ds.test
/main

# Font files
fonts/

# Userspace binaries (built by task)
/attrtest
/rachel
/handletest
/linux
/clocks
/clocktest

# Userspace priest binaries (built by task)
/disk
/fontsvc
/prefs
/slicer
/uitest

# Standalone tool binaries
/compile-constraints
/fix-go-elf
/mkfat32
/mkext2
/elf2pe
/relocate-kmazarin
/go-start
/gen-ast-stubs
/gen-overlay
/maz-reloc
/helloworld

# Stray shepherd/tool binaries
# (Created when `go build ./maz/<name>` is run from the repo root
# instead of going through `$GO tool task`. The Taskfile writes to
# build/ — never edit this list to "allow" a root binary.)
/fs
/stdio
/keymapper
/calc
/linux-ui
/maildb
/mail-ui
/versai
/fti
/mail

# Old directories (should be removed)
src/
flash/
/enable-mazzy

# Personal / runtime mail data (never committed — pre-existing .gitkeep
# placeholders and the gmail sample stay tracked because git ignores
# rules don't untrack files, but new content like current.mbox/ or
# .DS_Store is blocked.)
data/mail/

# Local project prefix (per-repo disambiguation)
.project-prefix

# Local API key config (never committed)
enable-deepseek

# Local CLAUDE.md modifications
CLAUDE.md


# sophie secrets (MAZ-43) — real API keys live locally; .example templates committed
/sophie/secrets/**/*.toml
!/sophie/secrets/**/*.toml.example

# slopstop: exclude .claude CONTENTS, not the directory. git cannot re-include a path whose
# parent directory is excluded, so `.claude/` would make the negations below inert and the
# generated skills would never be committed — defeating the version freeze they exist for.
#
# agents/ is exempted ahead of BILL-486, which may ship per-tier subagent definitions there.
# Cost if that ticket closes wontfix: nothing, the directory simply stays absent. Cost of
# adding it late: definitions land on disk, never get committed, and effort silently inherits
# while the config reads as configured — the same silent shape that left one repo with zero
# tracked skills.
.claude/*
!.claude/rules
!.claude/skills
!.claude/agents
