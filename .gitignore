# Build output
build/

# Documentation (managed outside git)
docs/

# Binary artifacts
*.o
*.elf
*.test
coverage.out

# Tool environments
tools/fontrender/myenv/

# Cross-compilation toolchain
aarch64-none-elf/

# Local binaries
bin/

# Generated constraint bytecode
*.vbc.go

# IDE/editor
.idea/
.vscode/
*.swp
*~

# Claude Code
.claude/

# Built .maz binaries
/fat32

# Task checksum cache
.task/

# Generated embedded data (regenerated by task)
cardinal/asm/dev/kmazarin_data_arm64.s
cardinal/asm/dev/boot_mazarin_data_arm64.s

# Test binaries in stack tests
cardinal/stack/*/main
cardinal/stack/*/test
cardinal/stack/test02_asm_bridge/test02_asm_bridge

# Compiled binaries that might appear in source dirs
kmazarin/kmazarin/kmazarin
# Staged embedded files (copied by stage tasks for go:embed)
kmazarin/kmazarin/fs.elf
kmazarin/kmazarin/kernel.toml
kmazarin/ds/ds.test
/main

# Font files
fonts/

# Userspace binaries (built by task)
/attrtest
/rachel
/handletest
/linux
/clocks
/clocktest

# Userspace priest binaries (built by task)
/disk
/fontsvc
/prefs
/slicer
/uitest

# Standalone tool binaries
/compile-constraints
/fix-go-elf
/mkfat32
/mkext2
/elf2pe
/relocate-kmazarin
/go-start
/gen-ast-stubs
/gen-overlay
/maz-reloc
/helloworld

# Stray shepherd/tool binaries
# (Created when `go build ./maz/<name>` is run from the repo root
# instead of going through `$GO tool task`. The Taskfile writes to
# build/ — never edit this list to "allow" a root binary.)
/fs
/stdio
/keymapper
/calc
/linux-ui
/maildb
/mail-ui
/versai
/fti
/mail

# Old directories (should be removed)
src/
flash/
/enable-mazzy

# Personal / runtime mail data (never committed — pre-existing .gitkeep
# placeholders and the gmail sample stay tracked because git ignores
# rules don't untrack files, but new content like current.mbox/ or
# .DS_Store is blocked.)
data/mail/

# Local project prefix (per-repo disambiguation)
.project-prefix

# Local API key config (never committed)
enable-deepseek

# Local CLAUDE.md modifications
CLAUDE.md


# sophie secrets (MAZ-43) — real API keys live locally; .example templates committed
/sophie/secrets/**/*.toml
!/sophie/secrets/**/*.toml.example
